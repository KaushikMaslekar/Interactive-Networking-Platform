import { SectionPage } from "../../components/section-page";

export default function LabsPage() {
  return (
    <SectionPage
      eyebrow="Labs"
      title="Browser-based networking labs"
      description="This area will eventually host device builders, packet flow observation, and connectivity debugging tools."
    />
  );
}
