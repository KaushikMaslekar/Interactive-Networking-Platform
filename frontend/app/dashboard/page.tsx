import { SectionPage } from "../../components/section-page";

export default function DashboardPage() {
  return (
    <SectionPage
      eyebrow="Dashboard"
      title="Learning progress dashboard"
      description="This area will summarize progress, quiz scores, streaks, achievements, and recent activity."
    />
  );
}
