import { SectionPage } from "../../components/section-page";

export default function ToolsPage() {
  return (
    <SectionPage
      eyebrow="Tools"
      title="Networking calculators and utilities"
      description="This area will contain subnet, CIDR, binary, DNS, HTTP, bandwidth, and latency tools."
    />
  );
}
